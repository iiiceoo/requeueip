---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: ippools.requeueip.io
spec:
  group: requeueip.io
  names:
    categories:
    - requeueip
    kind: IPPool
    listKind: IPPoolList
    plural: ippools
    shortNames:
    - rp
    singular: ippool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The IP version of IPPool.
      jsonPath: .spec.version
      name: VERSION
      type: string
    - description: The subnet to which IPPool belongs.
      jsonPath: .spec.subnet
      name: SUBNET
      type: string
    - description: The number of used IP addresses.
      jsonPath: .status.count.used
      name: USED
      type: string
    - description: The number of total IP addresses.
      jsonPath: .status.count.total
      name: TOTAL
      type: string
    - description: The age of IPPool.
      jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: IPPool is the Schema for the IPPools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IPPoolSpec defines the desired state of IPPool.
            properties:
              ranges:
                description: Ranges is the set of IP ranges allocated to this IPPool.
                items:
                  type: string
                type: array
              subnet:
                description: Subnet specifies the Subnet to which this IPPool belongs.
                type: string
              version:
                description: Version specifies the IP version (IPv4 or IPv6) of the
                  IPPool.
                enum:
                - IPv4
                - IPv6
                type: string
            required:
            - ranges
            - subnet
            - version
            type: object
          status:
            description: IPPoolStatus defines the observed state of IPPool.
            properties:
              count:
                description: Count is the count status of the IPPool.
                properties:
                  free:
                    description: Free is the number of available IP addresses in the
                      IPPool.
                    type: string
                  total:
                    description: Total is the total number of IP addresses in the
                      IPPool.
                    type: string
                  used:
                    description: Used is the number of used IP addresses in the IPPool.
                    type: string
                required:
                - free
                - total
                - used
                type: object
              free:
                description: Free is the current available IP ranges in the IPPool.
                items:
                  type: string
                type: array
            required:
            - free
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
