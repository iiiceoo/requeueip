---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: subnets.requeueip.io
spec:
  group: requeueip.io
  names:
    categories:
    - requeueip
    kind: Subnet
    listKind: SubnetList
    plural: subnets
    shortNames:
    - rn
    singular: subnet
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The IP version of Subnet.
      jsonPath: .spec.version
      name: VERSION
      type: string
    - description: The CIDR of Subnet.
      jsonPath: .spec.cidr
      name: CIDR
      type: string
    - description: The minimum unit of IP address assignments from Subnet.
      jsonPath: .spec.blockSize
      name: BLOCK-SIZE
      type: integer
    - description: The number of used IP blocks.
      jsonPath: .status.blockCount.used
      name: BLOCK-USED
      type: string
    - description: The number of total IP blocks.
      jsonPath: .status.blockCount.total
      name: BLOCK-TOTAL
      type: string
    - description: The age of Subnet.
      jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Subnet is the Schema for the Subnets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SubnetSpec defines the desired state of Subnet.
            properties:
              blockSize:
                description: |-
                  The minimum unit of IP address assignments from Subnet. Defaults to 30
                  for IPv4 and 126 for IPv6.
                format: int32
                type: integer
              cidr:
                description: The CIDR of Subnet.
                type: string
              excluded:
                description: The IP ranges excluded from Subnet.
                items:
                  type: string
                type: array
              version:
                description: The IP version of Subnet.
                enum:
                - IPv4
                - IPv6
                type: string
            required:
            - cidr
            type: object
          status:
            description: SubnetStatus defines the observed state of Subnet.
            properties:
              blockCount:
                description: The IP block count status of Subnet.
                properties:
                  free:
                    description: The number of available IP blocks.
                    type: string
                  total:
                    description: The number of total IP blocks.
                    type: string
                  used:
                    description: The number of used IP blocks.
                    type: string
                required:
                - free
                - total
                - used
                type: object
              free:
                description: The current available IP ranges of Subnet.
                items:
                  type: string
                type: array
            required:
            - free
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
